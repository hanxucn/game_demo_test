# 33 人技能更新：DSL 可实现性盘点

> 生成日期：2026-09-12　对应：设计者 2026-09 定稿的人物属性与技能
> 状态：**数值已更新并生效**；技能分两批 —— A 类可直接录入，B 类需先扩引擎能力

## A 类：现有 DSL 可直接表达（17 条）

| 人物 | 技能 | 表达方式 |
|---|---|---|
| 黄忠 | 百步穿杨 | `active` + `once_per_turn` + `damage 2` 指定敌方人物 |
| 张飞 | 咆哮 | `on_play`：`modify` 敌方攻击 −1（本回合）+ `damage 1` |
| 郭淮 | 据守 | `on_play`：`adjacent_to:self` → `modify health +2` + `apply_status jia_dun_status` |
| 夏侯渊 | 迅疾突袭 | `on_play`：`apply_status xian_gong_status` + `qi_xi_status` |
| 马岱 | 西凉子弟 | `aura`：`tag:西凉` → `modify +1/+1`；关键词 先攻 |
| 宋宪 | 伏击 | 关键词 `qi_xi` |
| 向宠 | — | 关键词 `jia_dun` |
| 夏侯恩 | 剑侍 | `on_death`：`draw 1` |
| 蔡瑁 | 水攻 | `on_play`：`damage 1` 指定敌人 |
| 黄祖 | 顽强 | `on_damaged` + 条件（未阵亡）→ `heal 1` |
| 董卓 | 暴虐 | `on_play`：`draw 1` + `damage 2` 己方主将（**待确认是否自伤**） |
| 周瑜 | 离间 | `clash` + `discard` + `value_from_discarded:cost` 造成伤害 / `return_to_hand` 随机 |
| 丁奉 | 奋勇 | `cost_rule`（己方受伤≤3血人数 → 自己少费） |
| 诸葛亮 | 运筹帷幄 | `active`：`scry 3` + `draw 1`；`aura` 条件（手牌为 0）→ 免疫 |
| 黄月英 | 机巧出阵 | `on_play`：`summon` ×2 token；`cost_modifier` 手牌计谋 −1 |
| 袁绍 | 威望 | `turn_start` `summon` 弓兵；`on_play` `add_to_deck`（手牌/牌组）；`on_death` `send_to_deck` |
| 关平 | 勇武 | `aura` 条件（关羽在场）→ `modify +1/+1`；`on_death` 给关羽 `sheng_dun_status` |

## B 类：需要先扩引擎能力（16 条）

| 人物 | 技能 | 缺什么 |
|---|---|---|
| 关羽 | 水淹七军 | **技能禁用**（`silence` 动作，ACTIONS 里没有） |
| 陈宫 | 忠烈 | 同上（禁攻击+禁技能）；且"直到下个自己回合开始"需时长表达 |
| 曹彰 | 将驰 | **抉择**（二选一）机制 |
| 庞统 | 凤雏 | 同上 |
| 魏延 | 桀骜不驯 | **掷骰子**分支（5–6 / 3–4 / 1–2）+ 冲锋（=先攻？） |
| 华雄 | 威震四方 | **`on_kill` 时机**（TIMING 里没有）+ **击杀者引用** |
| 马超 | 铁骑突袭 | 同上 + 敌主帅"禁主公技"（`block_lord_skill` 是否已实现待查） |
| 貂蝉 | 倾国倾城 | **性别字段**（"男性角色"） |
| 华佗 | 青囊 | 驱散**全部负面**（现 `remove_status` 只认具体状态 id）+ 治疗值取**目标统率值** |
| 程昱 | 审时度势 | **牺牲己方**并按**被牺牲卡的当时血量**取值 |
| 陆抗 | 谦冲如常 | **把亡语授予别的单位** |
| 张苞 | 将风 | **AOE 攻击**（对每个攻<自己的单位各打一次） |
| 姜维 | 文武双全 | **抽到非某类牌为止** |
| 祖茂 | 替主 | 确认"守护"如何授予（`shou_hu` + `redirect_damage`） |
| 貂蝉/华佗 | （见上） | — |

## 待设计者确认

1. **蔡瑁 / 黄祖的阵营**：设计者未指定，现状「群雄」。保持还是改魏/吴？
2. **董卓「暴虐」伤害对象**：自己主公（同曹操「奸雄」自伤套路）还是敌方主公？
3. **B 类的 12 项引擎能力**：按什么顺序补？（建议先做 `silence` 与 `on_kill`，各自牵动 2 张卡）
