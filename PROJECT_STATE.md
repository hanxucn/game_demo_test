# 《酒话三国》项目状态 · 记忆快照

> 更新：2026-09-26 · **新会话请先读本文件**，再按需展开 `docs/gdd/` 与 `docs/00-tech-selection.md`。
>
> 🔧 **有明确待办时先读 `docs/BACKLOG-skills.md`** —— 人物技能 B 类（B-3~B-11）
> 与战吼前端的可执行交接清单，含逐项的改哪个文件、加什么字段、测试要点、
> 以及本项目实际踩过的 10 个陷阱。

---

## 0. 一句话

以三国为背景、**5 列 × 2 排破阵**为核心机制的阵营卡牌对战游戏；3 人团队，2–3 个月出可玩 demo；先上微信/抖音小游戏（横屏），后续可上 Steam。

---

## 1. 已定决策（不要重新讨论）

### 1.1 技术选型

| 项 | 结论 |
|---|---|
| 引擎 | **Cocos Creator 3.8 LTS + TypeScript**（不用 4.0 起步） |
| 备选 | Godot 4.7 仅在"小游戏不是 v1 硬指标"时成立（当前不是） |
| 排除 | Unity（小游戏太重）、Unreal（错配） |
| 架构 | `core` 纯 TS 零引擎依赖 + `client` Cocos 表现层 + `server` Go |
| 屏幕 | **横屏**，适配策略 **fit-height + 宽度自适应** |
| 平台 | 微信小游戏 + 抖音小游戏为 v1 硬指标，配套移动 App |

详见 `docs/00-tech-selection.md`、`docs/01-ai-assisted-delivery-plan.md`。

### 1.2 机制 ADR（12 条，完整表见 `docs/gdd/14-open-questions.md` §3）

| 编号 | 决策 |
|---|---|
| ADR-001 | 战场 **5 列 × 2 排**（4 列偏慢、6 列偏快） |
| ADR-002 | 架盾**不阻挡**对主将的攻击（主将由阵型保护） |
| ADR-003 | 架盾**仅前军生效** |
| ADR-004 | 横屏 |
| ADR-005 | 先手用掷点 + 后手传国玉玺 |
| ADR-006 | 进化卡只针对特定兵种，与站位无关 |
| ADR-007 | **费用绑定总价值，不绑定属性** |
| ADR-008 | 神射**不绕过**架盾 |
| ADR-009 | 主将不参与普通攻击 |
| ADR-010 | 回合不分阶段（自由顺序） |
| ADR-011 | 引擎 Cocos Creator 3.8 LTS |
| ADR-012 | `core` 纯 TS、零引擎依赖 |

### 1.3 平台与合规（红线）

| 项 | 状态 |
|---|---|
| 主体 | 已有企业/个体工商户主体 |
| 变现 | **计划内购** → 必须版号（数月级，已列为第 1 周启动项） |
| 类目 | 卡牌大概率落**牌类/角色类**（个人主体不可选，企业主体可） |
| 内容红线 | 抽卡概率必须公示；禁止类棋牌/赌博机制 |
| 小游戏包体 | 首包 ≤ 4MB、分包合计 ≤ 30MB |

---

## 1.4 引擎实现（core）✅ 已完成第一版

`core/` 是**纯 TypeScript 规则引擎**，零引擎依赖，Cocos 客户端与 Go 服务端共用。

| 命令 | 作用 | 当前结果 |
|---|---|---|
| `cd core && npm test` | 254 个测试（规则/引擎/回放/DSL/B 类技能/AI/数据校验/内容收口） | ✅ 254/254 |
| `npm run typecheck` | `tsc --noEmit` | ✅ 通过 |
| `npm run validate` | 卡牌数据校验（GDD 的 10 条规则） | ✅ 0 错误 / 62 警告 |
| `npm run verify:dsl` | 逐张真实卡牌的 DSL 端到端 | ✅ 102 通过 / 0 失败 |
| `npm run smoke` | 两个 AI 互打一局 + 回放校验 | ✅ 回放完全一致 |
| `npm run ai:report` | AI 对局统计报表（攻击构成 / 技能使用 / 决策耗时） | ✅ 20 局：43% 打人物 |

> **2026-09-23 补齐**：`docs/BACKLOG-skills.md` 的 B-3 ~ B-11 全部落地（抉择 / 性别 /
> 连续普攻 / 攻击时加成时序 / 批量驱散 / 牺牲取值 / 复制手牌技能 / 抽到非某类为止 / 护主），
> 卡池 122 → **123 张**（新建祖茂），测试 153 → **204**。详见 ADR-069 ~ ADR-073。
>
> **同日追加（ADR-072，设计者答复）**：类型定型收口（1 攻武将必须显式标注，向宠/黄权改回谋臣）、
> 祖茂改回 2 费 1/3、`condition.event: killed` 时机修复（拆 before/after 两趟）、程昱随机敌人结项。
>
> **2026-09-26 AI 重写（ADR-084）**：旧 AI 是 163 行贪心（只能看攻/血/费用），
> 已换成 **`core/src/ai/` 三层架构**：候选生成（合法性全部取自引擎）+ 局面评估（七项）+ 模拟选优
> （把每个候选真的交给 `applyAction` 跑一遍再打分）。卡组风格画像分速攻/中速/后期三档调权重，
> 每张卡的价值按 `2×费用+1` 的预算等式估算。实测攻击构成从 **10% 打人物** 变为 **43% 打人物**
> （90% 糊脸 → 会换牌了），动作 0 次被引擎拒绝。设计与调参见 `docs/gdd/16-ai.md`。
>
> 顺带修掉一处**判定漂移**：AI 自己抄了一份主公技门控、漏了「进言」封锁 →
> 门控下沉到 `rules.canUseLordSkill`，引擎与 AI 共用（同 `canUseUnitSkill` 的做法）。
> **凡"AI 与引擎各判一次"的地方都要警惕这类漂移**，`test/ai.test.ts` 有专门的不变量测试盯着。
>
> 设计者实机截图又逼出第二处评估缺口（同日修正）：**衍生物换真卡没算卡差** ——
> 马腾召唤的 2/1 西凉铁骑去换 2/2 蔡瑁，按「攻+血」只赚 1.6，输给 2 点打脸，于是 AI 全打脸。
> 已给「进得了卡组」的单位加 `cardEquityOf`（+2.0，判据复用 `NON_DECK_TYPES`），
> 现在同一个衍生物会先换掉 2/2、第二个再打脸。测试 254 → **256**。
>
> 同日第三处评估缺口（设计者要求「算最优攻击」）：3/3 与 4/5 面对 2/2 时**谁去解场完全同分**，旧版按格子顺序瞎选，导致大攻被浪费在解场上、主将少挨伤害。已加 `ready` 项（本回合未用的攻击力 = 过期作废的资源，取值 0.5×face 且必须小于 face），AI 现在把解场交给"刚好够用"的那个、大攻留给主将。测试 256 → **258**。

**唯一写入口**：`applyAction(state, ctx, action) → { ok, state, events }`。
客户端只消费 `events` 播动画，**不判断规则**。

### ⚠️ 引擎实测发现的设计问题

用纯基础兵种对局，**40 回合只掉了 1 点主将血** —— 棋盘容易被铺满，没人能破阵。

这说明 GDD 里的"三泄压阀"不是可选项：
1. **计谋直伤**（谋臣/战法/事件卡直接打主将）—— 引擎已支持，**卡池里还没有这类卡**
2. **粮尽** —— 已实现，但 30 张卡组在 40 回合内触发不到
3. **破阵奖励** —— 尚未实现

> **设计约束**：卡池必须包含足够数量的"绕过阵型"手段，否则对局僵持。

---

## 1.5 引擎驱动的演示 ✅

`prototype/battlefield.html` 已改为**由 core 驱动**：

```
用户操作 → Core.applyAction(state, ctx, action) → { ok, state, events }
                                                     ↓            ↓
                                                  重新渲染      播放动画
```

**交互**：
- **按住手牌拖到绿色格子放置**（不是点选-点放）
- 点我方单位 → 合法目标高亮 → 点目标发起攻击（伤害/反击/死亡由 core 结算）
- 主公技：**消耗 1 统率 / 每回合 1 次**，需要目标的进入选目标模式
- "空列 → 可打主将"：选中该列单位 → 通道高亮 + 主公条变绿框

**验证结果**（无头浏览器实测，0 报错）：
- 拖拽出牌：场上单位 0 → 1 → 2，统率正确扣除
- 点击攻击：目标血量 1 → 阵亡
- 主公技：统率 3 → 2，二次使用统率不变

**踩过的坑**：`dataset.col` 是字符串，与数字比较必须 `Number()`——这个 bug 曾导致"点敌方单位无法攻击"。

重建命令：
```bash
python3 tools/yaml2json.py && python3 tools/build-data-bundle.py
cd core && npm run build:browser
```

---

## 2. 已产出资产

| 路径 | 内容 | 状态 |
|---|---|---|
| `docs/00-tech-selection.md` | 引擎/技术栈选型决策 | ✅ |
| `docs/01-ai-assisted-delivery-plan.md` | AI 辅助下的交付计划与压缩系数 | ✅ |
| `docs/gdd/README.md` | 机制文档索引、术语表、与炉石 9 处差异 | ✅ |
| `docs/gdd/01-overview.md` | 设计支柱、天时地利人和、非目标 | ✅ |
| `docs/gdd/02-battlefield.md` | 5×2 战场、4 条攻击规则、主将保护、UI 约束 | ✅ |
| `docs/gdd/03-turn-flow.md` | 开局、回合结构、胜负、粮尽 | ✅ |
| `docs/gdd/04-resources.md` | 统率值、护甲、30HP 论证 | ✅ |
| `docs/gdd/05-cards.md` | 六类卡牌、字段模板、两条高费卡路线 | ✅ |
| `docs/gdd/06-keywords.md` | 12 关键词详解 + 价值表 | ✅ |
| `docs/gdd/07-troops.md` | 三兵种 + 进化链 | ✅ |
| `docs/gdd/08-heroes.md` | 四主公与主公技 | ✅ |
| `docs/gdd/09-bonds.md` | 羁绊（同场 + 对抗） | 草案 |
| `docs/gdd/10-skills-statuses.md` | 技能模型、12 状态、23 步结算时机表 | ✅ |
| `docs/gdd/11-events-tactics-jiuling.md` | 事件卡、战法卡、酒令 | 草案 |
| `docs/gdd/12-randomness.md` | 随机性 5 条硬规则 | ✅ |
| `docs/gdd/13-balance-data-model.md` | 总价值预算、技能定价、schema、DSL | ✅ |
| `docs/gdd/14-open-questions.md` | 待决项、待填清单、ADR | 持续更新 |
| `docs/gdd/15-card-visual.md` | 卡面视觉与动画规范 | ✅ |
| `docs/gdd/index.html` | 单页 HTML 阅读版 | 由脚本生成 |
| `data/keywords.yaml` | 12 关键词 | ✅ |
| `data/statuses.yaml` | 12 状态 | ✅ |
| `data/cards.yaml` | **仅基础兵种 + 传国玉玺**，人物卡待填 | 部分 |
| `prototype/battlefield.html` | 战场原型（竖版卡牌 + 出牌/攻击动画） | ✅ |
| `prototype/card-gallery.html` | 卡面画廊（大卡 56×78 / 小卡 46×64 / 色板） | ✅ |
| `prototype/style-lab.html` | 风格实验室（布局/材质/装饰/背景 四轴切换） | ✅ |
| `prototype/card-render.css` / `.js` | 卡面视觉与动画模块 | ✅ |
| `prototype/rules.js` | 纯规则逻辑（零 DOM） | ✅ |
| `prototype/rules-check.mjs` | 规则断言测试（10 条全过） | ✅ |
| `prototype/README.md` | 原型实测数据与结论 | ✅ |
| `tools/render-gdd.mjs` | MD → 单页 HTML 渲染器 | ✅ |

---

## 3. 机制要点速查

### 3.1 战场

- 每方 **5 列 × 2 排 = 10 格**；主将在**最远端**
- 空间顺序（外→内）：`主公 → 后军 → 前军 → 中线 → 前军 → 后军 → 主公`
- 进攻路径：我方某列 → 该列敌方前军 → 该列敌方后军 → 敌方主公

### 3.2 攻击规则（4 条）

1. 近战只能攻击**同列**
2. 目标列前军为空 → 打该列后军（**穿透**）
3. 该列两排皆空 → 可打主将（**破阵斩将**）
4. **架盾最高优先级**（可跨列；仅前军生效；不阻挡打主将）

### 3.3 数值模型

- 白板基准：**属性点 ≈ 2 × 统率 + 1**
- 总价值 = 属性点 + 关键词价值 + 技能价值 + 羁绊协同 − 负面代价
- **费用不绑定属性**；≥6 费卡必须走"低属性强技能"或"高属性无技能"路线之一
- 已抓出的问题：盾兵 1 费 1/2 + 架盾**超模 1 点**；弓箭手 1 费 1/1 + 神射**偏弱 0.5 点**

### 3.4 内容产能铁律

> **加一张新卡 = 只改数据文件，不改代码。**
> 若必须改代码，说明效果 DSL 需要扩展，先更新 `13-balance-data-model.md`。

---

## 4. 待决项（阻塞内容生产，不阻塞架构）

见 `docs/gdd/14-open-questions.md` §1.1：

| 编号 | 问题 |
|---|---|
| Q-07-1 | 盾兵最终数值（1 费 1/1 或 2 费 1/3） |
| Q-07-2 | 弓箭手最终数值（1 费 1/2 或 2 费 2/2） |
| Q-09-2 | 羁绊失效时血量是否最低保留 1 点 |
| Q-08-1 | 曹操主公技（号令 +2 攻 / 召唤 1/1 / 抽牌自伤） |
| Q-08-2 | 群雄主公（董卓暴虐 / 袁绍望族） |

---

## 4.5 视觉迭代交接（重要）

**当前会话的模型无图像输入能力**（`read_image` 报 "does not declare image input"，
`describe_image` 报 baseURL 配置错误），因此界面是在"看不见渲染结果"的状态下开发的，
用户判定为"连 demo 都不及格"。

**已准备交接**：
- `prototype/VISUAL-HANDOFF.md` — 给下一个（具备视觉能力的）模型的完整交接说明
- `prototype/shot.sh` — 一键截图脚本（已验证可用）

**已解决（2026-09-09）**：根因是 `~/.dsh/settings.yaml` 的 `llm-deepseek.models` 覆盖了内置目录，
未声明 `inputModalities`，导致模型被判为纯文本。已为以下模型补上 `["text","image"]`：
- `deepseek-v4.1-flash-expires-on-0910`（当前会话模型）
- `deepseek-v4-flash-vision-exp`

备份：`~/.dsh/settings.yaml.bak-20260909-005126`。配置按请求热加载，**无需重启**。

**现在可以看图了**，视觉迭代流程：
```bash
./prototype/shot.sh battlefield 736 414     # 或 1200 700
# 然后用 read_image 看 .screenshots/*.png
```

## 5. 进度与下一步

### 已完成

1. 引擎与技术栈选型（含 Godot 小游戏成本评估）
2. 完整机制设计文档（15 份）+ 数据层骨架
3. 战场原型（炉石式卡面 + 出牌/攻击动画）+ 规则断言（13/13 通过）
4. 卡面视觉定案：**统一 56×78**、立绘铺满 + 暗金古典 + 无装饰；主公改为侧边卡；背景为三国古战场叙事场景
5. 卡面视觉与动画规范定稿
6. 原型阶段视觉验收（2026-09-08）→ 用户判定"不及格"，进入视觉重做
7. **视觉重做**（2026-09-09，多轮）：修复识图能力后进入"截图 → 看图 → 改"闭环
   - 卡面：深底 → **浅底深框**（卡牌亮度 0.12 → 0.34，成功从背景跳出）
   - 布局：统一 56×78，手牌扇形展开
   - **主公回到阵型正前/正后方**（横条：头像 + 名 + 生命/护甲 + 主公技）
   - **新增"空列通道"可视化**：空列浮现金色光束 + 箭头，可直击时主公条亮绿框
   - 为容纳主公条，卡牌从 56×78 调整为 **47×66**（几何硬约束）
   - **修复对齐 bug**：透视变换导致四行缩放不一致 → 移除透视，改为光影表现纵深
   - 空格子改为桌面凹槽，去掉虚线框的线框感
   - 环境：SVG 手绘古战场场景（虚化做景深）+ 木纹桌面 + 金色包边面板 + 透视
   - 量化红线写入 `15-card-visual.md` §8.5：卡牌亮度 ≥0.30、背景 ≤0.20、差值 ≥+0.12

### 下一步（两条线并行）

| 线 | 负责 | 内容 | 完成标准 |
|---|---|---|---|
| A | AI/工程 | `core` 骨架 + 卡面渲染管线 | 用基础兵种跑通"两个 AI 互打一局"，金种子回放通过 |
| B | 策划（用户） | **第一批 12 张卡**（见 §6） | 能按模板填入 `data/cards.yaml` 并通过校验 |

**为什么并行**：架构与具体卡牌内容互相不阻塞；但**不要一次性设计全部 90 张卡**——schema 会在前 10 张卡上暴露问题，先设计完再改会返工。

### 再往后

1. Cocos 工程搭建 + 小游戏真机 Spike（前端）
2. 合规推进：软著 / 版号 / 备案 / 类目（后端 B，第 1 周启动）
3. 服务端骨架：账号 / 存档 / 排行榜

---

## 6. 第一批卡牌需求（12 张，用于压测 schema）

**目的**：每一张都要触发**不同的数据 schema 分支**，用来验证"加卡不改代码"。

| # | 类型 | 要求 |
|---|---|---|
| 1–4 | 武将 ×4（每阵营 1） | 分别带 **疾行 / 无双 / 连击 / 忠义** 四个关键词 |
| 5 | 谋臣 ×1 | 主动技：**单体伤害** |
| 6 | 谋臣 ×1 | 主动技：**群体控制**（震慑或混乱） |
| 7 | 事件卡 ×1 | 对称伤害（如灾年） |
| 8 | 事件卡 ×1 | 对称召唤（如黄巾之乱） |
| 9 | 战法卡 ×1 | 单体伤害 |
| 10 | 战法卡 ×1 | 位移（如暗渡陈仓） |
| 11 | 战法卡 ×1 | 增益或控制 |
| 12 | 进化卡 ×1 | 虎豹骑（草案已有，确认数值） |

**填写模板**：`docs/gdd/05-cards.md` §5.1
**自检工具**：`docs/gdd/13-balance-data-model.md` 的公式

---

## 7. 团队分工（3 人）

| 角色 | 负责 |
|---|---|
| 前端 | Cocos 工程、UI、动画、平台 SDK、包体与性能 |
| 后端 A（玩法） | `core` 规则引擎、数据管线、平衡模拟器 |
| 后端 B（服务端/合规） | Go 服务端、埋点、支付链路、**版号/备案推进** |

---

## 8. 验证命令

```bash
# 生成机制文档单页 HTML
node tools/render-gdd.mjs

# 规则断言测试（应输出 10 通过 / 0 失败）
node prototype/rules-check.mjs

# 打开战场线框原型
open prototype/battlefield.html
open prototype/card-gallery.html
```
